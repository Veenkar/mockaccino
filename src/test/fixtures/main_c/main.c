#include <stdio.h>

FUNC(int32, const, RAM) main(const int argc, char argv[]) {
    printf("Hello, World!\n");
    return 0;
}

FUNC(float, const, RAM) main2(const intx * const argc, char argv[]) {
    printf("Hello, World!\n");
    dupa();
    obj.sub = "xD";
    return 0;
}

implicitReturnTypeFunction(int arg);

#if ASDF
#ifdef SOMETHING
DUPA(float, const, RAM) main3(const intx * const argc, char argv[]) {
    printf("Hello, World!\n");
    dupa();
    obj.sub = "xD";
    return 0;
}
#endif

#endif

